ALTER TABLE `mp`.`COMMERCE_ITEM` ADD COLUMN `MERCHANT` varchar(255) NULL AFTER `INDEX`, CHANGE COLUMN `CREATION_DATE` `CREATION_DATE` datetime DEFAULT NULL AFTER `MERCHANT`, CHANGE COLUMN `UPDATE_DATE` `UPDATE_DATE` datetime DEFAULT NULL AFTER `CREATION_DATE`;